# Server-side default key. Never commit the real .env file.
GEMINI_API_KEY=your-gemini-api-key

# Optional: keep the trial database somewhere else in deployment.
# TRIAL_DB_PATH=/var/lib/shiny-agents/trials.sqlite3
